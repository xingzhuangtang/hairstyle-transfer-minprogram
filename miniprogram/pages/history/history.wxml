<!--pages/history/history.wxml-->
<view class="history-container">
  <view class="header">
    <view class="header-title">历史记录</view>
    <view class="header-desc" wx:if="{{isVip}}">保留 {{remainingDays}} 天</view>
    <view class="header-desc" wx:else>开通会员保留 45 天</view>
  </view>

  <view class="vip-notice" wx:if="{{!isVip}}" bindtap="goToMember">
    <view class="vip-notice-content">
      <view class="vip-icon">👑</view>
      <view class="vip-text">
        <text class="vip-title">开通陪跑会员</text>
        <text class="vip-desc">历史记录保留 45 天，随时查看</text>
      </view>
      <view class="vip-arrow">></view>
    </view>
  </view>

  <view class="history-list" wx:if="{{isVip}}">
    <view class="empty-state" wx:if="{{records.length === 0 && !loading}}">
      <view class="empty-icon">📋</view>
      <view class="empty-text">暂无历史记录</view>
      <view class="empty-desc">开始使用发型迁移功能后<br/>历史记录将在这里显示</view>
    </view>

    <view class="loading-state" wx:if="{{loading}}">
      <view class="loading-spinner"></view>
      <view class="loading-text">加载中...</view>
    </view>

    <view wx:for="{{records}}" wx:key="id" class="history-item">
      <!-- 结果图片预览区域 -->
      <view class="item-preview-group">
        <!-- 原始结果图片 -->
        <view class="preview-item" 
              catchtap="showDownloadMenu" 
              data-id="{{item.id}}" 
              data-type="result">
          <image src="{{item.result_image}}" mode="aspectFill" lazy-load binderror="onImageError" data-index="{{index}}" data-type="result" />
          <view class="preview-label">结果</view>
        </view>
        
        <!-- 素描图片（如果有） -->
        <view wx:if="{{item.has_sketch}}" 
              class="preview-item" 
              catchtap="showDownloadMenu" 
              data-id="{{item.id}}" 
              data-type="sketch">
          <image src="{{item.sketch_image}}" mode="aspectFill" lazy-load binderror="onSketchError" data-index="{{index}}" data-type="sketch" />
          <view class="preview-label">素描</view>
        </view>
        <view wx:else class="preview-item preview-placeholder">
          <view class="placeholder-text">无素描</view>
        </view>
      </view>
      
      <view class="item-info">
        <view class="item-title">
          <text class="service-type">{{item.service_type_text}}</text>
          <text class="record-id">#{{item.id}}</text>
        </view>
        <view class="item-time">{{item.created_at}}</view>
        <view class="item-countdown {{item.countdown_class}}">
          {{item.countdown_text}}
        </view>
      </view>
      <view class="item-arrow" bindtap="viewDetail" data-id="{{item.id}}">></view>
    </view>
  </view>

  <view class="non-premium-list" wx:if="{{!isVip}}">
    <view class="feature-item">
      <view class="feature-icon">📸</view>
      <view class="feature-text">
        <view class="feature-title">查看历史</view>
        <view class="feature-desc">随时查看发型迁移历史记录</view>
      </view>
    </view>
    <view class="feature-item">
      <view class="feature-icon">⏰</view>
      <view class="feature-text">
        <view class="feature-title">45 天保留</view>
        <view class="feature-desc">历史记录保留 45 天</view>
      </view>
    </view>
    <view class="feature-item">
      <view class="feature-icon">💾</view>
      <view class="feature-text">
        <view class="feature-title">下载保存</view>
        <view class="feature-desc">支持下载高清结果图片</view>
      </view>
    </view>
  </view>

  <view class="load-more" wx:if="{{hasMore && isVip}}">
    <button class="load-more-btn" bindtap="loadMore">加载更多</button>
  </view>

  <!-- 隐藏的canvas用于生成合并图片 -->
  <canvas canvas-id="combinedCanvas" style="position:fixed;left:-9999px;top:-9999px;width:800px;height:1600px;"></canvas>
</view>
