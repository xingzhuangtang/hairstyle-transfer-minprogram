<!--pages/login/login.wxml-->
<view class="login-container">
  <!-- Logo区域 -->
  <view class="logo-section">
    <image class="logo" src="/images/icons/logo.png" mode="aspectFit"></image>
    <text class="app-name">发型迁移</text>
    <text class="app-desc">AI智能发型虚拟试戴</text>
  </view>

  <!-- 登录方式选择 -->
  <view class="login-methods">
    <!-- 微信登录按钮 -->
    <button
      class="login-btn wechat-btn"
      open-type="getUserInfo"
      bindgetuserinfo="onWechatLogin"
      loading="{{wechatLoading}}">
      <image class="btn-icon" src="/images/icons/wechat.png"></image>
      <text>微信一键登录</text>
    </button>

    <!-- 手机号登录 -->
    <view class="phone-login">
      <view class="input-group">
        <image class="input-icon" src="/images/icons/phone.png"></image>
        <input
          class="input"
          type="number"
          maxlength="11"
          placeholder="请输入手机号"
          value="{{phone}}"
          bindinput="onPhoneInput" />
      </view>

      <view class="input-group">
        <image class="input-icon" src="/images/icons/code.png"></image>
        <input
          class="input code-input"
          type="number"
          maxlength="6"
          placeholder="请输入验证码"
          value="{{code}}"
          bindinput="onCodeInput" />
        <button
          class="code-btn"
          bindtap="sendCode"
          disabled="{{counting}}">
          {{counting ? countDown + 's' : '获取验证码'}}
        </button>
      </view>

      <button
        class="login-btn phone-btn"
        bindtap="onPhoneLogin"
        loading="{{phoneLoading}}">
        登录
      </button>
    </view>
  </view>

  <!-- 用户协议 -->
  <view class="agreement">
    <checkbox-group bindchange="onAgreementChange">
      <label class="agreement-label">
        <checkbox value="agree" checked="{{agreed}}" />
        <text class="agreement-text">
          我已阅读并同意
          <text class="link" bindtap="viewUserAgreement">《用户协议》</text>
          和
          <text class="link" bindtap="viewPrivacyPolicy">《隐私政策》</text>
        </text>
      </label>
    </checkbox-group>
  </view>

  <!-- 提示信息 -->
  <view class="tips">
    <text class="tip-text">• 新用户登录即赠送1000发丝</text>
    <text class="tip-text">• 会员享受50%消费折扣</text>
  </view>
</view>
